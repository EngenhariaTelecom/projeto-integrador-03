name: Build Windows Release

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        pip install --upgrade pip
        pip install pyinstaller Pillow ttkbootstrap pyserial

    - name: Build EXE with PyInstaller
      shell: powershell
      run: |
        pyinstaller `
          --noconfirm `
          --onefile `
          --windowed `
          --name "MonitorBateria" `
          --icon "bateria_app/assets/icons/icon.ico" `
          --add-data "bateria_app/assets;assets" `
          --add-data "bateria_app/baterias;baterias" `
          "bateria_app/main.py"

    - name: Fix dist/ path (matches project tree)
      shell: powershell
      run: |
        Remove-Item -Recurse -Force bateria_app/dist 2>$null
        Move-Item dist bateria_app

    - name: Install NSIS
      run: choco install nsis -y

    - name: Build Installer
      shell: powershell
      run: |
        & "C:/Program Files (x86)/NSIS/makensis.exe" "installer.nsi"

    - name: Upload Installer
      uses: actions/upload-artifact@v4
      with:
        name: MonitorBateriaInstaller
        path: installer.exe